# Local dev overrides (copy to .env.local — gitignored by Next.js automatically)
# This switches Prisma to SQLite so you don't need a running Postgres for local dev.

DATABASE_URL=file:./prisma/dev.db
JWT_SECRET=local-dev-secret-change-in-production
